from bpmn.models import Ontology


class ProcessUtils:

  def __init__(self):
    # self.newsroom_domain = Ontology.objects.find()
    # self.newsroom_process = Ontology.objects.find()

  @staticmethod
  def verify_missing_tasks(author, existing_tasks):
      # all missing_tasks = tasks - existing_tasks
      # return list
      pass

  @staticmethod
  def verify_task_author(author, task):
      # return True or correct with right author 
      pass 

  @staticmethod
  def verify_process_missing_tasks(process, existing_tasks):
      # all missing_tasks = tasks - existing_tasks
      # return list
      pass
